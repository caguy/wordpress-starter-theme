{% extends "base.twig" %}

{% block content %}

    {% for post in posts %}

        <article>
            <h1>{{post.title}}</h1>
            <p>{{post.date}}</p>
            {{post.content}}
        </article>

    {% endfor %}

{% endblock %}